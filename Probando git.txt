Esto sólo lo hago porque lo manda Miguel.
Otra vez manda Miguel tocar aquí.

Veeeenga, algo interesante:
	git init = arranca git en la carpeta
	git add . = añade las instantaneas a la base de datos
	git commit -m "Nombre" = nombra las nuevas instantáneas añadidas
	git log --oneline = muestra de forma reducida las versiones de los archivos